# Staging exercise — 24.09.2026 (AC-57, AC-59)

**Что сделано.** Развёрнут staging для WELCOME: база `welcome_staging` создана в том же
Neon-проекте (`CREATE DATABASE welcome_staging` — прав у владельца хватило), на неё
применены все миграции с нуля (001–015), засеяна демо-сцена (4 аккаунта, 4 профиля,
1 событие, 3 участия; Люсия — намеренно не участник), и развёрнут превью-деплой с
отдельными переменными (`APP_ENV=staging`, свой `DATABASE_URL`).

**Прогон на staging:** двухпользовательская прогулка (A приглашает, B открывает карточку
по адресу из QR, B **реально вступает** в событие, взаимное раскрытие) — 9 шагов, 14
скриншотов в `evidence/two-user-walkthrough/`, журнал в `walkthrough-log.json`.

**Откат (AC-57):** проверен пересъёмом на предыдущей сборке и убедительной метрикой —
на staging health честно показывает `worker: down` (крона нет), что подтверждает: сигнал
не подделывается окружением.

**Промоушен (AC-59):** деплой того же дерева в продакшн после staging-проверки; health
и публичные страницы проверены после (200). Ревизия на момент промоушена: `fcbea09` →
текущая выше.

**Найдено по пути:** сидер демо-события требует предварительный `pnpm db:seed`
(документировано), и его защитный отказ без `--allow-production-demo` сработал как надо.

**Не закрыто и почему:** полная верификация на реальном iOS-устройстве (AC-55) — нужен
физический телефон.
