# dependencies
node_modules/

# next.js build output
.next/
out/
next-env.d.ts

# env files (values never committed) — example files are tracked
.env
.env.*
!.env.example
!.env.deploy.example

# runtime artifacts (dev OTP log etc.)
.runtime/

# misc
.DS_Store
*.pem
*.tsbuildinfo
.vercel
.env*
.env.deploy.secrets

# Google OAuth client downloads (default filename) — a secret by definition.
client_secret_*.json
.secrets/
